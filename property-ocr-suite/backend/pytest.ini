[pytest]
# `pythonpath = .` puts the backend directory on sys.path, so tests import
# `app.*` directly. Without it pytest only adds tests/ (no __init__.py there),
# which is why every test file grew its own sys.path.insert line.
pythonpath = .
testpaths = tests
